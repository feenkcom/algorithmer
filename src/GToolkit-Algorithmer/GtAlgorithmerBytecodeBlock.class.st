"
! Bytecode block
Is a lowlevel construction block of the algorithmer on which the other blocks are built.
Should not be used directly
"
Class {
	#name : #GtAlgorithmerBytecodeBlock,
	#superclass : #GtAlgorithmerBlock,
	#category : #'GToolkit-Algorithmer-Core'
}
